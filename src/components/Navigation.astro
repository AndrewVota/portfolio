---
const BASE_PATH = "/";
const ABOUT_PATH = "/about/";
const PROJECTS_PATH = "/projects/";
const WORK_PATH = "/work/";

const isHome = Astro.url.pathname === BASE_PATH;
const isAbout = Astro.url.pathname === ABOUT_PATH;
const isProjects = Astro.url.pathname === PROJECTS_PATH;
const isWork = Astro.url.pathname === WORK_PATH;
---

<nav>
	<ul
		class="inline-flex flex-wrap section-padding gap-6 font-light text-text-color-unfocused"
	>
		<li
			class={`text-nowrap ${isHome ? "font-medium text-text-color" : ""}`}
		>
			<a href={BASE_PATH}>home</a>
		</li>
		<li
			class={`text-nowrap ${
				isAbout ? "font-medium text-text-color" : ""
			}`}
		>
			<a href={ABOUT_PATH}>about</a>
		</li>
		<li
			class={`text-nowrap ${
				isProjects ? "font-medium text-text-color" : ""
			}`}
		>
			<a href={PROJECTS_PATH}>projects</a>
		</li>
		<li
			class={`text-nowrap ${isWork ? "font-medium text-text-color" : ""}`}
		>
			<a href={WORK_PATH}>work</a>
		</li>
	</ul>
</nav>
